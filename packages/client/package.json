{
  "name": "@weft/client",
  "version": "0.1.0",
  "private": true,
  "description": "Weft's client: the ProseMirror binding (S3), the React shell, the session state machine, the IndexedDB store with its memory fallback (S4). Runtime dependencies, each named and justified in the README: the six ProseMirror packages are the editor (design §5.3); react and react-dom are the shell. Vite is dev-only.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/headless.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "vitest run --coverage && vitest run --config vitest.config.latency.ts",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "prosemirror-commands": "^1.7.2",
    "prosemirror-keymap": "^1.2.3",
    "prosemirror-model": "^1.25.11",
    "prosemirror-state": "^1.4.4",
    "prosemirror-transform": "^1.12.1",
    "prosemirror-view": "^1.42.3",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  }
}
